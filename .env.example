# Database (local PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app_db

# Auth secret for JWT signing (change this to a random string in production)
AUTH_SECRET=portinel-change-this-to-a-random-secret

# That's it! No Supabase or external services required for local/Docker use.
